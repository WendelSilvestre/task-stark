name: invoiceCreate
on:
  schedule:
    - cron: '* * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
    curl --request POST \
        --url 'http://localhost:3000/src/pages/api/cron.ts' \
        --header 'Authorization: Bearer ${{ secrets.API_SECRET_KEY }}'